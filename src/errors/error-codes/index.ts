/**
 * Re-export error codes from the common constants module.
 * This module serves as the single source of truth for error code definitions
 * within the errors domain.
 */
export { ERROR_CODES, type ErrorCodeKey, type ErrorCodeDefinition } from '@common/constants/error-codes';
